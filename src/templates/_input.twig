{% import "_includes/forms" as forms %}

{% for item in lynx %}
    {{ forms.select( item.typeSelectSettings ) }}
    {{ forms.elementSelect( item.entrySelectSettings ) }}
    {{ forms.textField( item.textInputSettings ) }}
    {{ forms.textField( item.titleInputSettings ) }}
    {{ forms.textField( item.urlInputSettings ) }}
    {{ forms.checkboxField( item.targetCheckboxSettings ) }}
{% endfor %}